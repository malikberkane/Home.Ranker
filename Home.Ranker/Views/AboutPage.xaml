<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="Home.Ranker.Views.AboutPage">





    <Grid RowDefinitions="*,*,*">

        <StackLayout Grid.Row="1">
            <Label Text="Importance level" HorizontalOptions="CenterAndExpand" />
            <Slider
                Value="{Binding CurrentCriteria.RateValue, Mode=TwoWay}"
                Maximum="10"
                Minimum="1" />
            <Label x:Name="RateDescriptionLabel" VerticalOptions="CenterAndExpand" />

        </StackLayout>

        <Button
            Text="Validate"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            Clicked="Button_Clicked"
            Grid.Row="2" />
    </Grid>

</ContentPage>
